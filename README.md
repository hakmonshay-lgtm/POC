## NBA – Next Best Action Marketing Tool (MVP)

The generated app lives in `nba/`.

### Run locally

```bash
cd nba
npm install
npm run db:migrate
npm run dev
```

Then open `http://localhost:3000`.

